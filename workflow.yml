name: malaria app
on: push
jobs:
   my_app:
     runs-on: ubuntu-latest
     steps:
      - name: checkout Repo
        uses: actions/checkout@v6

      - name: List and Read file
        run: echo "malaria app Github Actions Job"
          
      - name: List files
        run: ls 
      - name: Read file
        run: cat README.md

    


      